{
  "version": 1,
  "notes": [
    "Catalog of OTA subsystem packages this server offers. The loader reads only `updates`.",
    "Each entry's `file` is resolved under the data dir (ETCO_ota_dataDir); length + sha1 are",
    "computed from the real file at startup, so an entry whose file is absent is skipped.",
    "fromVersion '*' matches any installed version, with a loop-guard: a package is never offered",
    "once the robot already runs toVersion. GetUpdateFrom serves the highest toVersion.",
    "filter '' is a wildcard (matches whatever otaFilter the robot sends).",
    "13.0.1 is the jibo.io repoint build of platform 13.0.0. It exists as a distinct, higher",
    "version because a robot already running 13.0.0 would never be offered 13.0.0 again \u2014 the",
    "loop-guard would suppress exactly the update that performs the repoint.",
    "13.0.6 adds independent @be/be 11.0.1 and oobe-config 9.0.1 skill OTAs; neither",
    "replaces the skills partition or removes the other installed skills.",
    "The 13.0.6 packages are published for filterless, fcs (SSM scheduler), and eau (the OOBE skill) requests. The source filter rule is a PREFIX match when the request carries a filter and an EXACT empty-string match when it does not; filter '' is NOT a wildcard."
  ],
  "updates": [
    {
      "id": "os-13.0.3",
      "subsystem": "os",
      "fromVersion": "*",
      "toVersion": "13.0.3",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v7. Everything in 13.0.2, plus /etc/ssl/cert.pem -> certs/ca-certificates.crt: OpenSSL's compiled-in default CAfile, which the stock image never shipped. Without it the native Poco client in jibo-jetstream-service verifies against no roots at all and every hub connection fails (\"certificate verify failed\"), so the robot reaches Classic but cannot stream a single audio turn. Also: the CA-accepting jibo-server-client in all six copies with its CA beside each, and client configs world-readable so the unprivileged skill user can read them.",
      "filter": "",
      "dependencies": {},
      "file": "os-13.0.3.tar"
    },
    {
      "id": "os-13.0.3-fcs",
      "subsystem": "os",
      "fromVersion": "*",
      "toVersion": "13.0.3",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v7. Everything in 13.0.2, plus /etc/ssl/cert.pem -> certs/ca-certificates.crt: OpenSSL's compiled-in default CAfile, which the stock image never shipped. Without it the native Poco client in jibo-jetstream-service verifies against no roots at all and every hub connection fails (\"certificate verify failed\"), so the robot reaches Classic but cannot stream a single audio turn. Also: the CA-accepting jibo-server-client in all six copies with its CA beside each, and client configs world-readable so the unprivileged skill user can read them.",
      "filter": "fcs",
      "dependencies": {},
      "file": "os-13.0.3.tar"
    },
    {
      "id": "services-13.0.3",
      "subsystem": "services",
      "fromVersion": "*",
      "toVersion": "13.0.3",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v7. Everything in 13.0.2, plus /etc/ssl/cert.pem -> certs/ca-certificates.crt: OpenSSL's compiled-in default CAfile, which the stock image never shipped. Without it the native Poco client in jibo-jetstream-service verifies against no roots at all and every hub connection fails (\"certificate verify failed\"), so the robot reaches Classic but cannot stream a single audio turn. Also: the CA-accepting jibo-server-client in all six copies with its CA beside each, and client configs world-readable so the unprivileged skill user can read them.",
      "filter": "",
      "dependencies": {
        "os": "13.0.3"
      },
      "file": "services-13.0.3.tar"
    },
    {
      "id": "services-13.0.3-fcs",
      "subsystem": "services",
      "fromVersion": "*",
      "toVersion": "13.0.3",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v7. Everything in 13.0.2, plus /etc/ssl/cert.pem -> certs/ca-certificates.crt: OpenSSL's compiled-in default CAfile, which the stock image never shipped. Without it the native Poco client in jibo-jetstream-service verifies against no roots at all and every hub connection fails (\"certificate verify failed\"), so the robot reaches Classic but cannot stream a single audio turn. Also: the CA-accepting jibo-server-client in all six copies with its CA beside each, and client configs world-readable so the unprivileged skill user can read them.",
      "filter": "fcs",
      "dependencies": {
        "os": "13.0.3"
      },
      "file": "services-13.0.3.tar"
    },
    {
      "id": "os-13.0.4",
      "subsystem": "os",
      "fromVersion": "*",
      "toVersion": "13.0.4",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v8. Everything in 13.0.3, plus two fixes a real OTA attempt exposed: (1) the OTA downloader (a separate Node 6 script from jibo-server-client) now passes an explicit CA, without which every update download failed instantly with UNABLE_TO_GET_ISSUER_CERT_LOCALLY and a 0-byte file; (2) no HubClient.override is baked, because it wins over region-settings and forced every robot onto api.jibo.io -- the Classic entrypoint, not a hub -- so audio turns went to the wrong service. Each robot now resolves the hub for its own region.",
      "filter": "",
      "dependencies": {},
      "file": "os-13.0.4.tar"
    },
    {
      "id": "os-13.0.4-fcs",
      "subsystem": "os",
      "fromVersion": "*",
      "toVersion": "13.0.4",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v8. Everything in 13.0.3, plus two fixes a real OTA attempt exposed: (1) the OTA downloader (a separate Node 6 script from jibo-server-client) now passes an explicit CA, without which every update download failed instantly with UNABLE_TO_GET_ISSUER_CERT_LOCALLY and a 0-byte file; (2) no HubClient.override is baked, because it wins over region-settings and forced every robot onto api.jibo.io -- the Classic entrypoint, not a hub -- so audio turns went to the wrong service. Each robot now resolves the hub for its own region.",
      "filter": "fcs",
      "dependencies": {},
      "file": "os-13.0.4.tar"
    },
    {
      "id": "services-13.0.4",
      "subsystem": "services",
      "fromVersion": "*",
      "toVersion": "13.0.4",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v8. Everything in 13.0.3, plus two fixes a real OTA attempt exposed: (1) the OTA downloader (a separate Node 6 script from jibo-server-client) now passes an explicit CA, without which every update download failed instantly with UNABLE_TO_GET_ISSUER_CERT_LOCALLY and a 0-byte file; (2) no HubClient.override is baked, because it wins over region-settings and forced every robot onto api.jibo.io -- the Classic entrypoint, not a hub -- so audio turns went to the wrong service. Each robot now resolves the hub for its own region.",
      "filter": "",
      "dependencies": {
        "os": "13.0.4"
      },
      "file": "services-13.0.4.tar"
    },
    {
      "id": "services-13.0.4-fcs",
      "subsystem": "services",
      "fromVersion": "*",
      "toVersion": "13.0.4",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v8. Everything in 13.0.3, plus two fixes a real OTA attempt exposed: (1) the OTA downloader (a separate Node 6 script from jibo-server-client) now passes an explicit CA, without which every update download failed instantly with UNABLE_TO_GET_ISSUER_CERT_LOCALLY and a 0-byte file; (2) no HubClient.override is baked, because it wins over region-settings and forced every robot onto api.jibo.io -- the Classic entrypoint, not a hub -- so audio turns went to the wrong service. Each robot now resolves the hub for its own region.",
      "filter": "fcs",
      "dependencies": {
        "os": "13.0.4"
      },
      "file": "services-13.0.4.tar"
    },
    {
      "id": "os-13.0.5",
      "subsystem": "os",
      "fromVersion": "*",
      "toVersion": "13.0.5",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v9. Everything in 13.0.4, plus the three fixes that let a freshly flashed or OTA'd robot come up working with nothing done by hand: file ownership is preserved (an unprivileged debugfs extract had handed the whole filesystem to uid 1000 = avahi, which broke sshd and every skill launch); setuid bits are restored (debugfs cannot create them, so /bin/busybox shipped 0755 and skills died with a bare 'Error launching app'); and an SSL shim is preloaded into jibo-server-service, which otherwise rejects every certificate and emits a NUL byte in its request headers that modern nginx 400s.",
      "filter": "",
      "dependencies": {},
      "file": "os-13.0.5.tar"
    },
    {
      "id": "os-13.0.5-fcs",
      "subsystem": "os",
      "fromVersion": "*",
      "toVersion": "13.0.5",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v9. Everything in 13.0.4, plus the three fixes that let a freshly flashed or OTA'd robot come up working with nothing done by hand: file ownership is preserved (an unprivileged debugfs extract had handed the whole filesystem to uid 1000 = avahi, which broke sshd and every skill launch); setuid bits are restored (debugfs cannot create them, so /bin/busybox shipped 0755 and skills died with a bare 'Error launching app'); and an SSL shim is preloaded into jibo-server-service, which otherwise rejects every certificate and emits a NUL byte in its request headers that modern nginx 400s.",
      "filter": "fcs",
      "dependencies": {},
      "file": "os-13.0.5.tar"
    },
    {
      "id": "services-13.0.5",
      "subsystem": "services",
      "fromVersion": "*",
      "toVersion": "13.0.5",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v9. Everything in 13.0.4, plus the three fixes that let a freshly flashed or OTA'd robot come up working with nothing done by hand: file ownership is preserved (an unprivileged debugfs extract had handed the whole filesystem to uid 1000 = avahi, which broke sshd and every skill launch); setuid bits are restored (debugfs cannot create them, so /bin/busybox shipped 0755 and skills died with a bare 'Error launching app'); and an SSL shim is preloaded into jibo-server-service, which otherwise rejects every certificate and emits a NUL byte in its request headers that modern nginx 400s.",
      "filter": "",
      "dependencies": {
        "os": "13.0.5"
      },
      "file": "services-13.0.5.tar"
    },
    {
      "id": "services-13.0.5-fcs",
      "subsystem": "services",
      "fromVersion": "*",
      "toVersion": "13.0.5",
      "created": 1789000000000,
      "changes": "jibo.io repoint build v9. Everything in 13.0.4, plus the three fixes that let a freshly flashed or OTA'd robot come up working with nothing done by hand: file ownership is preserved (an unprivileged debugfs extract had handed the whole filesystem to uid 1000 = avahi, which broke sshd and every skill launch); setuid bits are restored (debugfs cannot create them, so /bin/busybox shipped 0755 and skills died with a bare 'Error launching app'); and an SSL shim is preloaded into jibo-server-service, which otherwise rejects every certificate and emits a NUL byte in its request headers that modern nginx 400s.",
      "filter": "fcs",
      "dependencies": {
        "os": "13.0.5"
      },
      "file": "services-13.0.5.tar"
    },
    {
      "id": "os-13.0.6",
      "subsystem": "os",
      "fromVersion": "*",
      "toVersion": "13.0.6",
      "created": 1790116200000,
      "changes": "jibo.io OTA v13.0.6: preserve the proven 13.0.5 OS payload, including executable permissions and TLS support, and fix the embedded release reporter so the update is not offered again after installation.",
      "filter": "",
      "dependencies": {},
      "file": "os-13.0.6.tar"
    },
    {
      "id": "os-13.0.6-fcs",
      "subsystem": "os",
      "fromVersion": "*",
      "toVersion": "13.0.6",
      "created": 1790116200000,
      "changes": "jibo.io OTA v13.0.6: preserve the proven 13.0.5 OS payload, including executable permissions and TLS support, and fix the embedded release reporter so the update is not offered again after installation.",
      "filter": "fcs",
      "dependencies": {},
      "file": "os-13.0.6.tar"
    },
    {
      "id": "services-13.0.6",
      "subsystem": "services",
      "fromVersion": "*",
      "toVersion": "13.0.6",
      "created": 1790116200000,
      "changes": "jibo.io OTA v13.0.6: preserve the proven 13.0.5 services payload, bake verified public-CA handling into system backup and restore, and fix the embedded release reporter.",
      "filter": "",
      "dependencies": { "os": "13.0.6" },
      "file": "services-13.0.6.tar"
    },
    {
      "id": "services-13.0.6-fcs",
      "subsystem": "services",
      "fromVersion": "*",
      "toVersion": "13.0.6",
      "created": 1790116200000,
      "changes": "jibo.io OTA v13.0.6: preserve the proven 13.0.5 services payload, bake verified public-CA handling into system backup and restore, and fix the embedded release reporter.",
      "filter": "fcs",
      "dependencies": { "os": "13.0.6" },
      "file": "services-13.0.6.tar"
    },
    {
      "id": "be-11.0.1-jibo-io",
      "subsystem": "@be/be",
      "fromVersion": "*",
      "toVersion": "11.0.1",
      "created": 1790116200000,
      "changes": "Install the known-working Phoenix BE 11.0.1 in the standard @be/be skill slot, with jibo.io client endpoints and public CA. Does not replace the skills partition.",
      "filter": "",
      "dependencies": { "os": "13.0.6", "services": "13.0.6" },
      "file": "be-11.0.1-jibo-io.tar"
    },
    {
      "id": "be-11.0.1-jibo-io-fcs",
      "subsystem": "@be/be",
      "fromVersion": "*",
      "toVersion": "11.0.1",
      "created": 1790116200000,
      "changes": "Install the known-working Phoenix BE 11.0.1 in the standard @be/be skill slot, with jibo.io client endpoints and public CA. Does not replace the skills partition.",
      "filter": "fcs",
      "dependencies": { "os": "13.0.6", "services": "13.0.6" },
      "file": "be-11.0.1-jibo-io.tar"
    },
    {
      "id": "be-11.0.1-jibo-io-from-12",
      "subsystem": "@be/be",
      "fromVersion": "12.0.0",
      "toVersion": "11.0.1",
      "created": 1790116200000,
      "changes": "Replace original-cloud BE 12.0.0 with the hardware-tested Phoenix BE 11.0.1; this is an intentional compatibility downgrade.",
      "filter": "",
      "dependencies": { "os": "13.0.6", "services": "13.0.6" },
      "file": "be-11.0.1-jibo-io.tar"
    },
    {
      "id": "be-11.0.1-jibo-io-from-12-fcs",
      "subsystem": "@be/be",
      "fromVersion": "12.0.0",
      "toVersion": "11.0.1",
      "created": 1790116200000,
      "changes": "Replace original-cloud BE 12.0.0 with the hardware-tested Phoenix BE 11.0.1; this is an intentional compatibility downgrade.",
      "filter": "fcs",
      "dependencies": { "os": "13.0.6", "services": "13.0.6" },
      "file": "be-11.0.1-jibo-io.tar"
    },
    {
      "id": "oobe-config-9.0.1-jibo-io",
      "subsystem": "oobe-config",
      "fromVersion": "*",
      "toVersion": "9.0.1",
      "created": 1790116200000,
      "changes": "Update the OOBE skill's own server client, endpoints, and public CA without replacing the rest of the skills partition.",
      "filter": "",
      "dependencies": { "os": "13.0.6", "services": "13.0.6" },
      "file": "oobe-config-9.0.1-jibo-io.tar"
    },
    {
      "id": "oobe-config-9.0.1-jibo-io-fcs",
      "subsystem": "oobe-config",
      "fromVersion": "*",
      "toVersion": "9.0.1",
      "created": 1790116200000,
      "changes": "Update the OOBE skill's own server client, endpoints, and public CA without replacing the rest of the skills partition.",
      "filter": "fcs",
      "dependencies": { "os": "13.0.6", "services": "13.0.6" },
      "file": "oobe-config-9.0.1-jibo-io.tar"
    },
    {
      "id": "os-13.0.6-eau",
      "subsystem": "os",
      "fromVersion": "*",
      "toVersion": "13.0.6",
      "created": 1790116200000,
      "changes": "jibo.io OTA 13.0.6 for the stock OOBE update channel; corrected embedded version reporter and preserved executable metadata.",
      "filter": "eau",
      "dependencies": {},
      "file": "os-13.0.6.tar"
    },
    {
      "id": "services-13.0.6-eau",
      "subsystem": "services",
      "fromVersion": "*",
      "toVersion": "13.0.6",
      "created": 1790116200000,
      "changes": "jibo.io OTA 13.0.6 for the stock OOBE update channel; backup/restore TLS and corrected embedded version reporter.",
      "filter": "eau",
      "dependencies": { "os": "13.0.6" },
      "file": "services-13.0.6.tar"
    },
    {
      "id": "be-11.0.1-jibo-io-eau",
      "subsystem": "@be/be",
      "fromVersion": "*",
      "toVersion": "11.0.1",
      "created": 1790116200000,
      "changes": "Install hardware-tested BE 11.0.1 into the standard @be/be slot through the stock OOBE update channel.",
      "filter": "eau",
      "dependencies": { "os": "13.0.6", "services": "13.0.6" },
      "file": "be-11.0.1-jibo-io.tar"
    },
    {
      "id": "be-11.0.1-jibo-io-from-12-eau",
      "subsystem": "@be/be",
      "fromVersion": "12.0.0",
      "toVersion": "11.0.1",
      "created": 1790116200000,
      "changes": "Replace original-cloud BE 12.0.0 with the hardware-tested Phoenix BE 11.0.1 through the stock OOBE update channel.",
      "filter": "eau",
      "dependencies": { "os": "13.0.6", "services": "13.0.6" },
      "file": "be-11.0.1-jibo-io.tar"
    },
    {
      "id": "oobe-config-9.0.1-jibo-io-eau",
      "subsystem": "oobe-config",
      "fromVersion": "*",
      "toVersion": "9.0.1",
      "created": 1790116200000,
      "changes": "Update the OOBE skill's own jibo.io client and public CA through the stock OOBE update channel.",
      "filter": "eau",
      "dependencies": { "os": "13.0.6", "services": "13.0.6" },
      "file": "oobe-config-9.0.1-jibo-io.tar"
    }
  ]
}
