# Moved to [docs/internal/HW-OOBE-TEST.md](docs/internal/HW-OOBE-TEST.md)

This hardware test runbook is an internal build record; it moved during the documentation refresh. A few code comments and the task ledger in `docs/parity/` still cite this
path, so this pointer stays until those references are updated; the content itself is
maintained only at the link above.
