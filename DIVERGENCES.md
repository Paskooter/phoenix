# Moved to [docs/DIVERGENCES.md](docs/DIVERGENCES.md)

The deliberate-differences register moved here during the documentation refresh. A few code comments and the task ledger in `docs/parity/` still cite this
path, so this pointer stays until those references are updated; the content itself is
maintained only at the link above.
