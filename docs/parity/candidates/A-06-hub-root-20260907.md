# A-06 Hub transport and request failure: root acceptance

Root accepts `b037a05` plus the `406d28d` boundary repair, reviewed at integration `83836e1c43fb410d8f34ecb85c6082289b2bf020`. The Account-to-Hub path now honors original friendly-ID and transaction-header handling, smart JSON parsing, GET redirects and deadlines, ordinary non-2xx response data, and provider error status/code fields. A missing or invalid status in a Hub error now produces one HTTP 500 while the Settings listener remains available for the next request. The pre-repair Hub candidate's process exit is retained as failed evidence.

Root compared fresh integrated captures with completed original Node 8 captures. All **eight complete service-response cases** agree, including a successful following request in every case. All **40 payload/transport controls** and **11 redirect/deadline controls** also agree on selected results, error structures and ordered wire requests. The service controls run actual original App.start/Hapi and recovered Account/Hub clients against controlled TCP peers. The 40/11 matrices exercise the clients directly; registry discovery and Person/Lasso are named seams in the service controls.

The frozen integrated tree passes **632 unit tests**, with 3 skips and no failures, tracker validation, and all **43 strict smoke cases** with zero differences, invariants or coverage gaps. Runtime and tests match the submitted candidate, the accepted parser tree is unchanged, and the worktree had its own verified dependency links. A root attempt to start a fresh reference container exhausted its budget before any source code ran; it remains a tooling failure, while preserved completed source captures are the acceptance basis.

Only validated reset log-marker timestamps and listener authorities in redirect traces are replaced for comparison. Complete response structures, status, codes, message presence, nulls, and behavior remain checked. Harmless diagnostic wording follows the user's compatibility policy.

[Root review](../evidence/2026-09-07/settings-hub/review.json) and [comparison](../evidence/2026-09-07/settings-hub/comparison.json). Full A-06, deployment authentication, live provider/OAuth/Mongo behavior and physical robot acceptance remain open. Additional whole-service transport controls are being reviewed separately.
