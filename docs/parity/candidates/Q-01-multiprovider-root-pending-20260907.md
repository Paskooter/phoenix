# Q-01 multi-provider candidate — changes requested

The isolated candidate adds an explicit Bing/Wikipedia/Wolfram profile.
Root reran 36 complete source/candidate response and recovery cases using
MarkupSafe 1.0 and the actual Unidecode 1.0.22 package. All 36 agree. The
compatible setuptools build tooling fixes the earlier dependency setup failure;
it does not establish an exact historical build environment.

The two added late-answer cases prove that Bing or Wikipedia can win after the
first group's three-second deadline while Wolfram is still pending. All 36
source and candidate phase traces pass; six corrupted timing controls fail.
The earlier six original Node 8 Hub client framing controls remain pinned.

Root found substantive faults in the separate Unicode candidate: dot-only
Unicode mappings are erased inside strings, and the adapter does not trim dots
before checking unhelpful prefixes. These change whether a reply is spoken.
Five of eight compound-string predicate challenges differ. Luna is confirming
and repairing them through the original Python 3.6 provider. Generated data
provenance and transport-exception recovery also remain under review.

The profile has not been integrated or deployed. Q-01, account/attribution,
live provider access, full corpus coverage and robot acceptance remain open.
The [pending review](../evidence/2026-09-07/gqa-multiprovider-pending/review.json)
retains both the earlier qualified run and the exact-dependency follow-up.
