> Root accepted the bounded GQA core slice; see the [integration review](Q-01-core-integration-root-20260907.md). Candidate history below retains its original scope and qualifications.

# Q-01 GQA response and filtering repair

Root-reviewed candidate; main integration and deployed robot acceptance are still pending. The parent Q-01 task remains open.

The original GQA service is a Flask endpoint. Wrapping it with the generic BaseSkill route changed uncaught result-shape failures from HTTP 500 into HTTP 200 ERROR frames and discarded its provider timing fields. This candidate uses GQA's own response/error boundary, restores the original blocked-term branch before provider calls, and preserves the source behavior for malformed provider result objects.

The reference is `srv-gqa-ws@ebe1a7d38f511570060c1fbf61bec89d58419b26`, version 5.2.15. Root compared 12 complete route responses and eight additional provider-output boundaries with the actual original Python 3.6.15/Flask 0.12.2 handlers. All 20 status codes and normalized complete bodies agree. Four HTTP status errors are repaired; the blocked-term response and the source array payload shape are also restored. Many prior full-body mismatches concerned timing fields and are not counted as independent product features.

Only generated message/JCP IDs, nonnegative elapsed duration values, HTTP 400 diagnostic HTML, and HTTP 500 diagnostic message/stacktrace wording are normalized. ID format and uniqueness are checked; timing keys remain compared. Speech, display, action, analytics, skill version, final/fireAndForget fields, and error status/body shape remain visible to the comparison. Original captures are retained. The success media-type difference remains a qualified transport difference supported by the prior original Axios 0.17.1 parsing control.

The original 203 partial patterns, 32 whole-word patterns, 62 whitelist patterns, and four blocked-term MIM prompts are preserved. The JavaScript matcher agrees with 473 independently executed original Python 3.6 results covering list entries, repetitions, whitelist/case behavior, embedding, and Unicode boundaries. The retained test fixture contains those original results rather than values computed by Phoenix.

Validation: all 163 skill tests pass, including the eight provider result HTTP boundaries, the 473-result matcher control, filter precedence/provider suppression, and preservation of handler timing fields. Private root captures and exact execution receipts are under `.parity/reviews/q01-gqa-core-root-20260907`.

Qualifications: the source runtime required MarkupSafe 1.1.1 after the locked 1.0 installation failed; the failure and fallback receipts are retained. Provider, account, attribution, and some NLP dependencies remain named controlled seams. Source-control attempts with a missing timing seed and then an undefined fixture clock are retained as invalid harness runs; the valid final control supplies the source-required provider fork timestamp. This slice does not accept those missing dependencies, the Wikipedia preprocessing approximation, the complete Q-01 task, or robot execution. The unusual source array-valued speech payload is preserved at the HTTP boundary without claiming that the robot can play it.
