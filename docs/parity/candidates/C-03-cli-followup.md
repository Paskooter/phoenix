# C-03 CLI follow-up candidate

This is an unverified, isolated follow-up from `082d58651315a285b966399b7b227916d529975f`. It adds the source common-runner behavior at the Phoenix executable boundary: missing, synchronous, and rejected startup results are logged and schedule status-1 exit after the source five-second interval. The programmatic `start`, argument parsing, and port defaults remain unchanged. Help still derives its usage basename from the invoked path.

The implementation is in `packages/skills/src/index.js`, with injected-hook tests in `packages/skills/test/reportConfig.test.js` and a CommonJS process probe at `packages/skills/tools/c03-cli-exec-probe.cjs`. The probe measures its child with `process.hrtime()` so the timings below do not use Docker wall time.

The original report executable and common runner are pinned to source revision `5c0a7390539663ba749d360de348a428c088505c`. Their source hashes are recorded in the JSON report. For process controls, the source used the cached Node 8.9.4 executable whose SHA-256 is `03841801a7957b0eb5e2dbb1257eeda9d3edd18291d18d03bd57515c83107c5d`; it is the same byte-pinned executable used in the native evidence, run as a host process rather than labeled as Docker. The candidate ran on Node 22.22.0.

Both direct cases exited with status 1 and completed without a probe timeout. For `--help`, source elapsed time was 7579.790 ms and candidate elapsed time was 5138.841 ms. Both emitted the same source error line, `Service didn't return promise`, but source also emitted Jibo logging-config/NewRelic setup output and reported `run-service.js`, while the candidate reported the actual invoked `index.js` basename. For `--port notaport`, source elapsed time was 5315.237 ms and candidate elapsed time was 5129.225 ms. Both reached the delayed error path, while their Node/runtime error text and stacks differ: Node 8 reports the source `RangeError: "port" argument must be >= 0 and < 65536`; Node 22 reports its current NaN validation wording. The complete JSON receipts and hashes are in `.parity/reviews/c03-cli-followup-082d586-20260906/cli-process-comparison.json`.

The candidate intentionally leaves source `init-newrelic`, local Jibo logging-config writes, and their environment-dependent warnings outside this bounded wrapper change. Earlier Docker attempts are retained as infrastructure-timeout evidence and are not used for timing claims. The focused report configuration file has 10 passing tests, and `npm run test:unit` has 430 passing tests with no failures. Workspace links resolve to this worktree; the dependency setup receipt records that its `node_modules` was copied from the already installed frozen `082d586` candidate, without changing the shared lockfile.

The candidate remains unverified pending root review. No live ports, robot, main, tracker, or integration changes were made.
