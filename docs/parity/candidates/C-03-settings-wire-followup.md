# C-03 Settings transport follow-up candidate

This unverified follow-up starts from `61f6b1c449d441e3d2abb5d3b0ef81c9ed428a59` in the fresh worktree `.parity/worktrees/c03-settings-wire-followup`. It keeps the Settings-only source headers and private `node:http`/`node:https` boundary from that candidate, then repairs the source-observable response behavior that the transport controls exposed. The helper now follows the pinned Axios 0.17.1 `follow-redirects` method/body/header rules, decodes the source adapter's gzip/deflate responses, applies its permissive JSON transform, preserves non-2xx response data on rejection, and returns the partial body at the same truncated-response boundary. No global HTTP client was changed.

The private differential runner exercised the pinned compiled source `SettingsClient` and this candidate against separate loopback peers. It used the extracted Node 8.9.4 executable (`03841801a7957b0eb5e2dbb1257eeda9d3edd18291d18d03bd57515c83107c5d`) for the source and Node 22.22.0 for the candidate. The caller fixture uses a source-shaped numeric birthdate and `runtime.location.iso`; this makes the adult caller path reachable instead of accidentally taking the source default path with a JavaScript date string. Both direct `getSettings` and caller `getUserPrefs` were run for eleven scenarios, twenty-two cases total.

All twenty-two parsed outcomes match after removing runtime, stack, and dynamic response-date fields. All twenty-two captured request sequences match after replacing only the independently assigned loopback Host value. For 301, 302, and 303, the source changes the initial POST to a GET and removes entity headers/body. For 307, it retains POST, entity headers, and the exact body. Gzip and deflate responses decode to the same settings data. Invalid and empty JSON remain raw strings under the source transform; the reachable caller consequently produces the source `res.find is not a function` or conversion error. 400 and 500 reject with the source message and status/data response fields. A declared-length truncated response returns the bytes received so far, and the caller then follows the same source error path.

The old-fetch control is retained separately in `.parity/reviews/c03-settings-wire-followup-20260906/response-controls-old-fetch.json`. It already followed redirects and decompressed valid gzip/deflate responses, but differed from Axios on malformed/empty JSON, non-2xx error shape, and truncated response handling. The pre-repair `61f6b1` control is retained as `response-controls-before-transport-repair.json`; it additionally failed all eight redirect cases and four encoded-response cases because the new private helper had removed fetch's behavior. The current changes therefore repair both regressions introduced by the private transport and the source response-transform/error gaps found in the same bounded matrix.

Focused report configuration tests pass 10/10, and the complete repository unit suite passes 450/450. The full source/client receipt, raw request/response captures, normalized comparison, old-fetch control, and commands are under `.parity/reviews/c03-settings-wire-followup-20260906/`. Source execution is the extracted host Node 8 binary; the pinned Node 8 image is provenance only and was not started for this control. Synthetic loopback providers were used; no deployed Account, Hub, Person, Lasso, robot, live service, main worktree, or shared lockfile was changed. Redirect-loop limits, cross-origin redirects, DNS/TLS failures, and deployed provider behavior remain unverified.
