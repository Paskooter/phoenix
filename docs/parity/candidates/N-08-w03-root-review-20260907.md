# N-08 wildcard separator proposal: changes requested

Root rejected `700e40c` for integration. It repairs two209-row corpus requests,
but introduces a selected-intent regression in a native-confirmed contrast:

```text
TopRule = ((a $w03 c){% intent='w03' %}|(a $* c){% intent='star' %});
input: a one c
```

The preserved native execution selects `w03`. Root independently confirmed
the accepted baseline also selects `w03`, while the proposal selects `star`.
Charging separators only to `$w03` changes its ranking against `$*`.
The agent is repairing the interaction through general native arc semantics;
no phrase/rule-pair exception or full-replay acceptance is allowed.

The [review](../evidence/2026-09-07/nlu-w03-rejected/review.json) retains exact
outputs and provenance. Main retains51 residuals. Harmless diagnostic wording
is nonblocking; this rejection concerns a changed selected intent.
