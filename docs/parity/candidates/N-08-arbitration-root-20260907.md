# N-08 factory membership and arbitration: root acceptance

Root accepts frozen `e3965821155ccaf159d43a7c9ccffd85700220da`, integrated at `b42c0a8eaf56b5319b7b9faedf9d19d952a3b0cb` with identical NLU runtime and resource bytes. The complete default AST replay now matches **20,471 of 20,528** original HTTP status/data responses: **57 differences**, 11 repairs from the preceding 68, no newly failing cases, and no changed outputs among the 57 residuals. Against the earlier 149-difference baseline, 92 failing cases are repaired and none are newly failing.

The finite first-name list incorrectly included `my`, which let introductions consume it as a name and beat the expected chitchat result. A fresh root execution of the pinned original native parser accepts all 6,007 remaining entries and rejects `my`. This checks every vendored entry, not exhaustive equality with the native factory language. The earlier agent receipt reported a different executable hash; it remains preserved, while this acceptance uses the fresh root run and verified archived binary hash.

Both parser profiles now share the source result-selection rule: prefer the larger supplied score, retain request/result order on ties, and remove `launch` or `globals/*` from a tie when a non-loser is present. Thirteen fresh controls against the actual Pegasus `RobustParserClient.getBestResult` module on Node 8 agree. These controls use finite supplied scores; the AST scorer remains an approximation, and arbitrary nonfinite inputs are outside this acceptance.

The full replay exited 0 after 2,762.237 seconds within its 7,200-second budget, without timeout or termination. Root reconciled the complete result and verified unchanged code, resources, input and harness hashes. The integrated tree passes **620 unit tests**, with 3 skips and no failures, plus all **43 strict smoke cases** with zero differences, invariants or coverage gaps. An initial comparison-controller JSON decoding error was repaired in the private harness and rerun; the failed attempt remains recorded and does not count as passing evidence.

[Root review](../evidence/2026-09-07/nlu-arbitration/review.json) and [full replay reconciliation](../evidence/2026-09-07/nlu-arbitration/comparison.json) retain the exact scope and hashes. This is not a new full-production or robot acceptance. Full N-08 stays open, and the separate portable graph-data candidate remains unverified.
