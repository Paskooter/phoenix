# Moved to [docs/CLASSIC-SERVICES.md](docs/CLASSIC-SERVICES.md)

The robot-cloud API inventory moved here during the documentation refresh. A few code comments and the task ledger in `docs/parity/` still cite this
path, so this pointer stays until those references are updated; the content itself is
maintained only at the link above.
